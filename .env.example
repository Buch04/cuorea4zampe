# Example env file — do NOT commit secrets. Copy to .env and fill values.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_user@example.com
SMTP_PASS=your_smtp_password

EMAIL_FROM="Cuore a 4 Zampe <noreply@example.com>"
EMAIL_TO=recipient@example.com
